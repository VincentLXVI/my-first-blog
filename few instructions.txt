
https://tutorial.djangogirls.org/ru/installation/


python -m venv myenva

python -m venv myenvb
python -m venv myenvc

Запусти виртуальное окружение, выполнив:
C:\Users\Name\DjangoEnv> myenv\Scripts\activate

(myvenv) ~$ python3 -m pip install --upgrade pip
pip install django~=1.11.0

python manage.py runserver 0:8000

git status
git add --all .
git commit -m "comment"
git push
Открой вкладку "терминалы" на PythonAnywhere и переключись на уже запущенную консоль Bash (или новую). Затем набери следующую команду:
command-line
$ cd ~/my-first-blog
$ git pull
[...]